@if(Request.IsAuthenticated) {
    <text>Chào mừng <strong>@Html.ActionLink(RentalHouseFinding.Common.CommonModel.GetNameOfUser(User.Identity.Name), "Index", "User")</strong>! ( @Html.ActionLink(RentalHouseFinding.Common.CommonModel.GetUserNotification(User.Identity.Name) + " thông báo", "Index", "Questions", null, new { @style = "color:red" }) )
     @Html.ActionLink(" | Trang admin", "Index", "Admin").If(User.IsInRole("Admin"))
    | @Html.ActionLink("Thoát", "LogOff", "Account")</text>
}
else {
    @:[ @Html.ActionLink("Đăng nhập", "LogOn", "Account") ]
    @:[ @Html.ActionLink("Đăng ký", "Register", "Account") ]
}
